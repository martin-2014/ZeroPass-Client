import styles from './index.less';

const Sign = () => {
    return (
        <div className={styles.sign}>
            <img className={styles.img} src="./icons/poweredby.png" />
        </div>
    );
};

export default Sign;
